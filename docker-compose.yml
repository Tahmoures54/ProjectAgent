version: "3.8"
services:
  web:
    build: .
    container_name: project_agent_web
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
      - ./logs:/app/logs
